# ----------------------------------------------------------------------------
# Group: Group 3 — COMP 4350: Software Engineering 2
# Project: Studly
# Author: Hamed Esmaeilzadeh (team member)
# Generated / scaffolded with assistance from ChatGPT (GPT-5 Thinking mini)
# Date: 2025-10-07
# ----------------------------------------------------------------------------
name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (apps/backend)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node from .nvmrc if present else 20
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: 'npm'

      - name: Install backend deps
        run: npm ci
        working-directory: apps/backend

      - name: Run backend tests
        run: npm test
        working-directory: apps/backend

      - name: Build backend (if you have a build step)
        run: npm run build --if-present
        working-directory: apps/backend

  frontend:
    name: Frontend (apps/frontend)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node from .nvmrc if present else 20
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: 'npm'

      - name: Install frontend deps
        run: npm ci
        working-directory: apps/frontend

      - name: Run frontend tests
        run: npm test
        working-directory: apps/frontend

      - name: Build frontend (Vite)
        run: npm run build
        working-directory: apps/frontend

      - name: Upload frontend build as artifact (optional)
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: |
            apps/frontend/build
            apps/frontend/dist
