# apps/frontend/.env.example
# Copy to .env.local for local Docker compose

# Backend base URL reachable from the browser
VITE_API_BASE_URL=https://studly-backend-server-production.up.railway.app/api

# Must match backend INTERNAL_API_TOKEN for local usage
VITE_RAILWAY_API_TOKEN=studly-local-token
