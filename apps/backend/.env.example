# apps/backend/.env.example
# Copy to .env.local for local Docker compose

# Token expected by backend middleware (x-api-key)
INTERNAL_API_TOKEN=studly-local-token

# Optional: Supabase credentials if you want real auth locally
# SUPABASE_URL=https://<your>.supabase.co
# SUPABASE_ANON_KEY=ey...

# Optional: password reset redirect target
# PASSWORD_RESET_REDIRECT_URL=http://localhost:5173/reset-password

